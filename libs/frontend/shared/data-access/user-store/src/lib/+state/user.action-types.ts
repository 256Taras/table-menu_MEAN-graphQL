/**
 * @description
 *
 * User action type
 */

export enum UserTypes {
  LoadUser = '[User] Load User',
  LoadUserCancel = '[User] Load User Cancel',
  LoadUserRun = '[User] Load User Run',
  LoadUserSuccess = '[User] Load User Success',
  LoadUserFailure = '[User] Load User Failure'
}
