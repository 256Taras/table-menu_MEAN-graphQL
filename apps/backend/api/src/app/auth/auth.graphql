extend type Query{
  "It's login query demo description"
  login(
    "Username it's name current user"
    username:String,
    "password it's password current user"
    password:String
  ):SingInresponce
  logout:Boolean
}


"""
Demo
multi-line

"""
type SingInresponce {
  accessToken:String
  expiresIn:Int
  id:Int
}
